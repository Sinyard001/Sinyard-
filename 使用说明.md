# 我的笔记 · 简历工作台 使用说明

这是一款**本地运行**的笔记 + 简历工具，数据保存在你自己的电脑里，不会上传到任何服务器。

## 一、怎么启动

**双击文件夹里的 `启动.bat`（或 `我的笔记.lnk`）**，程序会在后台**最小化运行**，并在就绪后**自动打开浏览器**，像打开一个小网站一样。

- 用完后点软件右上角的 **「⏻ 退出」** 即可关闭（也可以关掉任务栏里那个最小化的小窗口）。
- 下次再用，还是双击 `启动.bat`（或桌面的快捷方式）。
- 如果浏览器没有自动打开，手动在浏览器地址栏输入：`http://127.0.0.1:8787`
- 如果黑色窗口提示「端口 8787 已被占用」，说明已经开着一个了，把之前那个黑色窗口关掉再试。

> 注意：直接双击 `index.html` 虽然也能看到界面，但飞书同步和 AI 生成会不可用，页面顶部会给出红色提示。完整使用请务必双击 `启动.bat`。

### 想放到桌面，点一下就用

在文件夹里**右键 `启动.bat` → 「发送到」→「桌面快捷方式」**，桌面上就会出现「我的笔记」图标；以后双击桌面图标即可。

## 二、五个功能页

1. **首页**：每天自动同步你飞书多维表格里「昨天更新」的内容，也可以随手写笔记。
2. **经历提炼**：填一句口语化的大学习历，AI 帮你生成书面、专业的简历用语，可一键复制或存入「用语库」。
3. **简历模板**：填好简历内容，套用「简约 / 现代 / 经典 / 商务 / 学术」五种模板，实时预览；**编辑时会自动存档防止丢失**，可导出 PDF 或 Word（.doc），还能对「个人总结 / 经历要点」一键 **AI 润色**。
4. **岗位定制**：粘贴岗位 JD，AI 会综合你写过的**所有简历 + 「经历大汇总」**，自动写出一份完整、匹配该岗位的简历，可另存或导出。
5. **面试**：输入你的经历，AI 生成可直接使用的面试回答；或设想面试官会问的问题；也可以把面试官的问题贴进来，直接得到合格的答案。

## 三、首次使用需要配置（重要）

点右上角「⚙️ 设置」，填两项：

### 1）飞书多维表格（用于首页同步「昨日更新」）

需要飞书开放平台的「自建应用」。步骤如下：

1. 打开 https://open.feishu.cn/app ，登录后点击「创建企业自建应用」，填个名字（如“我的笔记”）。
2. 进入应用，在「凭证与基础信息」里复制 **App ID** 和 **App Secret**，填到本软件设置里。
3. 在「权限管理」里搜索 `bitable`，开通 **「查看、评论、编辑和管理多维表格」**（bitable:app）权限。
4. 在「版本管理与发布」里创建一个版本并**发布**（让权限生效）。
5. 回到你的飞书多维表格，点右上角「分享」→「添加文档应用」，搜索并添加你刚创建的应用，权限至少给**「可阅读」**。

> App ID 类似 `cli_xxxxxxxx`。多维表格的 `app_token` 和 `table_id` 已按你提供的链接预填好，一般不用改。

### 2）AI 接口（用于生成简历用语 / 定制简历）

本软件兼容 OpenAI 接口格式，支持 OpenAI、DeepSeek、Kimi、通义千问、智谱等。

在设置里可以直接选「服务商快捷选择」自动填入，也可以手动填：

- **接口地址 Base URL**：例如 `https://api.openai.com/v1` 或 `https://api.deepseek.com/v1`
- **API Key**：在对应平台申请
- **模型名称**：例如 `gpt-4o-mini`、`deepseek-v4-flash`、`qwen-plus`、`kimi-k3`

> 如果用的是 Moonshot（Kimi），请使用模型 `kimi-k3`；旧的 `moonshot-v1-*` 系列已停止对新用户开放、将于近期下线。

填完三项后，可以点旁边的「🔌 测试连接」立即验证，成功会显示模型回复，失败会显示具体原因。

> 密钥只会保存在本机 `config.json` 文件里，仅在调用 AI 接口时使用。

## 四、数据存在哪里

- 笔记、简历、简历用语：保存在浏览器的本地存储（localStorage）里，仅本机可见。
- 飞书 / AI 配置：保存在同目录下的 `config.json` 文件里。

## 五、常见问题

- **首页提示“尚未配置飞书”**：到设置里填好飞书 App ID / Secret，并确认已把多维表格分享给该应用。
- **飞书同步报错 `scopes required: [bitable:app:readonly, ...]`**：说明飞书应用还没开通多维表格权限或没发布。到飞书开放平台 → 你的应用 →「权限管理」搜索 `bitable`，开通「查看、评论、编辑和管理多维表格」（bitable:app），再到「版本管理与发布」创建版本并**发布**；最后回到多维表格「分享 → 添加文档应用」把应用加进来。
- **AI 生成失败 / 显示「AI 不能用」**：到设置里点「🔌 测试连接」查看具体原因；常见原因是模型名失效（Kimi 请用 `kimi-k3`）、API Key 错误、或账户余额不足。
- **“昨日更新”为空**：说明昨天确实没有记录被修改，这是正常情况。

## 六、隐私说明

本软件完全在本地运行，飞书和 AI 的调用都由你自己的电脑直连对应平台。除你配置的平台外，不会把任何数据发送到其他地方。
